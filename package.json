{
  "name": "carnet",
  "private": true,
  "description": "Cabinet de Lecture et Atelier Crédit (sites statiques + fonctions Netlify)",
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
